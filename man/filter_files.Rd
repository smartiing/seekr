% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_files}
\alias{filter_files}
\title{Filter files to search}
\usage{
filter_files(
  path,
  ...,
  extension = NULL,
  path_pattern = NULL,
  max_file_size = Inf,
  exclude = seekr::exclude_functions,
  .progress = seekr_option("seekr.progress")
)
}
\arguments{
\item{path}{A character vector of file paths to filter.}

\item{...}{These dots are for future extensions and must be empty.}

\item{extension}{Optional character vector of file extensions to keep.
Either:
\itemize{
\item \code{NULL} (default): no filtering by extension; all extensions are kept.
\item A character vector of extensions to keep, with or without a leading dot
(e.g. \code{c("R", ".Rmd", "qmd")}).
}

Extensions are normalized before matching: leading dots are stripped,
matching is case-insensitive, and duplicates are ignored. Only the last
component of compound extensions is used (e.g. \code{"tar.gz"} uses \code{"gz"}),
with a warning.}

\item{path_pattern}{Optional pattern applied to filter normalized file paths (see
\code{\link[=as_seekr_path]{as_seekr_path()}}). Either:
\itemize{
\item \code{NULL} (default): no filtering by path.
\item A string, interpreted as a regular expression via \code{\link[stringr:regex]{stringr::regex()}}.
\item A \code{stringr_pattern} object such as \code{\link[stringr:regex]{stringr::regex()}} or
\code{\link[stringr:fixed]{stringr::fixed()}}.
}}

\item{max_file_size}{Maximum file size in bytes. Files larger than this
value are excluded. Default is \code{Inf}, meaning no files are excluded by
size. Zero and negative values are treated as \code{Inf}.}

\item{exclude}{Named list of functions used to exclude unwanted
files during filtering. Either:
\itemize{
\item \code{NULL}, to disable additional exclude functions.
\item A named list of functions, each taking a character vector of normalized
file paths and returning a logical vector of the same length, where
\code{TRUE} means the file should be excluded.
}

Defaults to \link{exclude_functions}, which excludes common non-text or
irrelevant files.}

\item{.progress}{Whether to display progress messages. Default is \code{TRUE} in
interactive sessions and \code{FALSE} otherwise (see \code{\link[rlang:is_interactive]{rlang::is_interactive()}}).
Can be set globally with \code{options(seekr.progress = FALSE)}.}
}
\value{
A character vector of normalized absolute paths that passed all filters.
Paths use the same representation as \code{\link[=as_seekr_path]{as_seekr_path()}}.

An attribute \code{"exclusions"} is always attached to the result, containing
a data frame with one row per input file and one column per exclusion function,
detailing which files were excluded and why. Retrieve it with \code{\link[=exclusions]{exclusions()}}.
}
\description{
\code{\link[=filter_files]{filter_files()}} keeps files matching \code{extension} and \code{path_pattern}, and
not exceeding \code{max_file_size}. Finally, the \code{exclude} functions are applied to
the remaining files, discarding common non-text or irrelevant files by default.
It is the second step of the \code{\link[=seek]{seek()}} pipeline, applied after \code{\link[=list_files]{list_files()}}
and before \code{\link[=match_files]{match_files()}}.

Exclusion filters are applied in this order:
\enumerate{
\item \code{extension}: keeps only files whose extension is in the provided list.
\item \code{path_pattern}: keeps files whose normalized path matches the pattern.
\item \code{max_file_size}: excludes files larger than the given size.
\item \code{\link{exclude_functions}}: applies each named function to the remaining files.
}

Files are only passed to each subsequent filter if they have not already been
excluded by a previous one.

Details about excluded files are stored on the result and can be retrieved
with \code{\link[=exclusions]{exclusions()}}.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
files <- list_files(path = ext_path)

# Keep only R files
filter_files(files, extension = "R")

# Keep only files in the R/ subfolder
filter_files(files, path_pattern = "/R/")

# Exclude files larger than 1 MB
filter_files(files, max_file_size = fs::fs_bytes("1MB"))

# Inspect which files were excluded and why
filtered <- filter_files(files, extension = "R")
exclusions(filtered)

# Disable default exclude functions
filter_files(files, exclude = NULL)

# Add a custom exclude function
my_fns <- exclude_functions
my_fns$generated <- function(path) grepl("/generated/", path)
filter_files(files, exclude = my_fns)

}
\seealso{
\itemize{
\item \code{\link[=list_files]{list_files()}} to produce the input paths.
\item \code{\link[=match_files]{match_files()}} to search the filtered files for a pattern.
\item \code{\link[=exclusions]{exclusions()}} to inspect which files were removed and why.
\item \code{\link[=seek]{seek()}} to run the full pipeline.
}
}
