% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seekr-match.R
\name{seekr_match}
\alias{seekr_match}
\alias{new_seekr_match}
\title{Create \code{seekr_match} vectors}
\usage{
new_seekr_match(
  path = character(),
  start_line = integer(),
  end_line = integer(),
  start = integer(),
  end = integer(),
  start_col = integer(),
  end_col = integer(),
  match = character(),
  replacement = character(),
  before = character(),
  line = character(),
  after = character(),
  encoding = character(),
  hash = character()
)
}
\arguments{
\item{path}{A character vector of source identifiers. For file workflows,
these are normalized absolute file paths. For \code{\link[=match_text]{match_text()}} workflows,
they may also be non-existing identifiers.}

\item{start_line, end_line}{Integer vectors. 1-based line numbers where each
match begins and ends.}

\item{start, end}{Integer vectors. 1-based absolute character positions where
each match begins and ends.}

\item{start_col, end_col}{Integer vectors. 1-based column positions of the
match start and end within their respective lines.}

\item{match}{A character vector. The exact text matched.}

\item{replacement}{A character vector. The staged replacement for each
match. \code{NA} indicates no replacement is staged.}

\item{before}{A character vector. Context lines preceding each match.}

\item{line}{A character vector. The complete line(s) containing each match.}

\item{after}{A character vector. Context lines following each match.}

\item{encoding}{A character vector. The encoding used to read each file.}

\item{hash}{A character vector. Hash of the searched text, used to
check that it has not changed before replacement.}
}
\value{
\code{new_seekr_match()} returns an empty or populated \code{seekr_match} vector.
\code{new_seekr_match()} is a low-level constructor intended primarily for internal
use and advanced extensions. In normal usage, create \code{seekr_match} vectors with
\code{\link[=seek]{seek()}}, \code{\link[=seekr]{seekr()}}, \code{\link[=match_files]{match_files()}}, or \code{\link[=match_text]{match_text()}}.
}
\description{
A \code{seekr_match} is an S3 vector built on \code{\link[vctrs:new_rcrd]{vctrs::new_rcrd()}} that represents
the matches found by \code{\link[=seek]{seek()}}, \code{\link[=seekr]{seekr()}}, \code{\link[=match_files]{match_files()}}, or \code{\link[=match_text]{match_text()}}.

Each element corresponds to a single match and stores its source path,
position, matched text, optional replacement, surrounding context lines,
encoding, and a hash of the searched text used for replacement safety.
}
\section{Fields}{

Access any field with \code{\link[vctrs:field]{vctrs::field()}} which is re-exported by seekr:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{field(x, "path")
field(x, "match")
}\if{html}{\out{</div>}}
}

\section{Methods and functions}{

\code{seekr_match} objects support the following S3 methods:
\itemize{
\item \link[=print.seekr_match]{print()}: displays matches in a formatted view with
optional context and replacement preview.
\item \link[=summary.seekr_match]{summary()}: summarizes matches by file, matched
text, replacement, extension, and encoding.
\item \link[=str.seekr_match]{str()}: shows the internal field structure with types
and sample values.
\item \link[=as_tibble.seekr_match]{tibble::as_tibble()}: converts to a tibble for
advanced manipulation.
}

The following functions are also commonly used with \code{seekr_match} vectors:
\itemize{
\item \code{\link[=as_match]{as_match()}}: converts a tibble back to a \code{seekr_match} vector.
\item \code{\link[=filter_match]{filter_match()}}: subsets matches using dplyr-style expressions.
\item \code{\link[=sort_within_files]{sort_within_files()}}: reorders matches within each file while
preserving file order.
}
}

\section{Attributes}{

In addition to its fields, a \code{seekr_match} vector returned by \code{\link[=seek]{seek()}} or \code{\link[=seekr]{seekr()}}
may carry two attributes:
\itemize{
\item \code{empty_stage}: when the vector is empty, indicates which pipeline step
produced no output. Retrieve with \code{\link[=empty_stage]{empty_stage()}}.
\item \code{exclusions}: when files were removed during filtering, a data frame
detailing which files were excluded and by which function. Retrieve with
\code{\link[=exclusions]{exclusions()}}.
}

These attributes are dropped when combining \code{seekr_match} vectors.
}

\examples{
# Produce a seekr_match vector
ext_path <- system.file("extdata", package = "seekr")
x <- seekr("function", toupper, path = ext_path)

# Access a field
field(x, "path")
field(x, "match")

# Subset
head(x, 3)

# Combine two seekr_match vectors
y <- seekr("Hello", path = ext_path)
c(x, y)

}
\seealso{
\itemize{
\item \code{\link[=seek]{seek()}} and \code{\link[=seekr]{seekr()}} to search for matches and produce \code{seekr_match}
vectors.
\item \code{\link[=print.seekr_match]{print.seekr_match()}} and \code{\link[=summary.seekr_match]{summary.seekr_match()}} to inspect results.
\item \code{\link[=filter_match]{filter_match()}} to subset matches before replacing.
\item \code{\link[=replace_files]{replace_files()}} to apply staged replacements.
\item \code{\link[=empty_stage]{empty_stage()}} and \code{\link[=exclusions]{exclusions()}} to diagnose empty results.
}
}
