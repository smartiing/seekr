% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{exclude_functions}
\alias{exclude_functions}
\alias{is_git_dir}
\alias{is_dependency_dir}
\alias{is_minified_file}
\alias{is_not_text_mime}
\title{Default file exclusion functions}
\format{
A named list of exclude functions.
}
\usage{
exclude_functions

is_git_dir(path)

is_dependency_dir(path)

is_minified_file(path)

is_not_text_mime(path)
}
\arguments{
\item{path}{A character vector of file paths to filter.}
}
\description{
\code{exclude_functions} is the default named list of functions used by
\code{\link[=filter_files]{filter_files()}} to exclude files that are usually not useful for text search.
}
\details{
Each function receives a character vector of normalized file paths and must
return a logical vector of the same length. \code{TRUE} means that the
corresponding file should be excluded.

The default pipeline includes:
\itemize{
\item \code{is_git_dir()}: excludes files located inside \verb{.git/}.
\item \code{is_dependency_dir()}: excludes files in common dependency folders such as
\verb{node_modules/}, \verb{renv/}, \verb{.venv/}, \verb{vendor/}, and \verb{__pycache__/}.
\item \code{is_minified_file()}: excludes minified or bundled files such as \code{.min.js},
\code{.bundle.css}, etc.
\item \code{is_not_text_mime()}: excludes files not recognized as text based on their
MIME type.
}

You can disable all exclude functions with \code{exclude = NULL}, remove
one of the defaults by setting it to \code{NULL} in a copy of
\link{exclude_functions}, or add your own named function to the list.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
files <- list_files(path = ext_path)
names(exclude_functions)

# Disable default exclude functions
filter_files(files, exclude = NULL)

# Add a custom exclude function
my_fns <- exclude_functions
my_fns$generated <- function(path) grepl("/generated/", path)
filter_files(files, exclude = my_fns)

}
\seealso{
\code{\link[=filter_files]{filter_files()}}
}
