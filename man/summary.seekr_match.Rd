% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summary.seekr_match}
\alias{summary.seekr_match}
\alias{print.summary_seekr_match}
\title{Summarize matches and planned replacements}
\usage{
\method{summary}{seekr_match}(object, ...)

\method{print}{summary_seekr_match}(x, ..., n = NULL)
}
\arguments{
\item{object}{A \code{\link{seekr_match}} vector.}

\item{...}{Not used. Present for compatibility with the \code{summary()} generic.}

\item{x}{A \code{summary_seekr_match} object, as returned by \code{\link[=summary.seekr_match]{summary.seekr_match()}}.}

\item{n}{Maximum number of rows to print in each summary table. If \code{NULL}, a
compact default is used. This limit is applied separately to each section of
the summary, such as top files, top matches/replacements, top extensions,
and top encodings.}
}
\value{
An object of class \code{summary_seekr_match}, containing summary tables for
files, matches/replacements, extensions, and encodings. Print it with
\code{print()} to display a formatted summary in the console.
}
\description{
\code{summary()} produces a compact overview of a \code{\link{seekr_match}} vector:
the most frequent files, matched texts, file extensions, and encodings.

When replacements are staged, matched texts are displayed together with their
replacement preview, giving a high-level picture of what \code{\link[=replace_files]{replace_files()}}
would change.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
x <- seekr("TODO", path = ext_path)
y <- seekr("TODO", "DONE", path = ext_path)
summary(x)
summary(y)
summary(c(x, y))

}
\seealso{
\itemize{
\item \code{\link[=print.seekr_match]{print.seekr_match()}} for a full match-level display with context lines.
\item \code{\link[=str.seekr_match]{str.seekr_match()}} for the internal field structure.
}
}
