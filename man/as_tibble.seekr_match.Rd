% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seekr-match.R
\name{as_tibble.seekr_match}
\alias{as_tibble.seekr_match}
\alias{as.data.frame.seekr_match}
\alias{as_match}
\title{Convert \code{seekr_match} vectors to and from data frames}
\usage{
\method{as_tibble}{seekr_match}(x, ...)

\method{as.data.frame}{seekr_match}(x, ...)

as_match(x)
}
\arguments{
\item{x}{For \code{as_tibble()} and \code{as.data.frame()}: a \code{\link{seekr_match}} vector.

For \code{as_match()}: a data frame with at least the columns listed in the
\strong{Fields} section of \link{seekr_match}. Additional columns are silently
ignored. All required columns must have the correct type: \code{character} for
string fields, \code{integer} for position fields.}

\item{...}{Not used. Present for compatibility with S3 generics.}
}
\value{
\code{as_tibble()} returns a \code{tbl_df}. \code{as.data.frame()} returns a plain
\code{data.frame}. In both cases the result has one row per match and one column
per field (see the \strong{Fields} section of \link{seekr_match}).

\code{as_match()} returns a \code{\link{seekr_match}} vector. Matches within each file are
sorted by position. Overlapping or incoherent matches within the same file
cause an error.

Note: the \code{empty_stage} and \code{exclusions} attributes of the original
\code{\link{seekr_match}} vector are not preserved through the conversion.
}
\description{
\code{as_tibble()} and \code{as.data.frame()} convert a \code{\link{seekr_match}} vector into a
tibble or plain data frame, with one row per match and one column per field.

\code{as_match()} is the reverse: it converts a data frame back into a
\code{\link{seekr_match}} vector, validating all fields and checking for overlapping
matches within each file before returning.

Together, these functions unlock the full tidyverse toolkit for manipulating
match metadata. A typical pattern is to convert to a tibble, use
\code{\link[dplyr:mutate]{dplyr::mutate()}} or \code{\link[dplyr:filter]{dplyr::filter()}} to derive or modify columns,
including the \code{replacement} field, and then convert back with \code{as_match()}
before calling \code{\link[=replace_files]{replace_files()}}.
}
\examples{
\dontrun{
library(dplyr)
ext_path <- system.file("extdata", package = "seekr")
x <- seekr("TODO", path = ext_path)

# Convert to tibble
df <- as_tibble(x)
df

# Convert to plain data frame
as.data.frame(x)

# Convert back to seekr_match
as_match(df)

# Set a replacement for all matches
df$replacement <- "DONE"
as_match(df)

# Suppose you want to replace `"foo"` with `"bar"`, but only for the last
# match in each file, and only in files with at least three matches:
x <- seekr("foo", "bar", path = ext_path)

y <-
  x |>
  as_tibble() |>
  mutate(
    ith_match_per_file_rev = n():1L,
    n_match_per_file = n(),
    .by = path
  ) |>
  filter(ith_match_per_file_rev == 1L, n_match_per_file >= 3L) |>
  as_match()

# replace_files(y)
}
}
\seealso{
\itemize{
\item \link{seekr_match} for the list of available fields.
\item \code{\link[=filter_match]{filter_match()}} for simpler subsetting that does not require conversion.
\item \code{\link[vctrs:field]{vctrs::field()}} to access or modify a single field in place.
\item \code{\link[=replace_files]{replace_files()}} to apply staged replacements after converting back.
}
}
