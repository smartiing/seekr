% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{as_seekr_path}
\alias{as_seekr_path}
\title{Normalize file paths for seekr}
\usage{
as_seekr_path(x)
}
\arguments{
\item{x}{A character vector of paths.}
}
\value{
A character vector of normalized absolute paths, with the same length and
order as \code{x}.
}
\description{
\code{as_seekr_path()} converts paths to the normalized format used internally
by seekr for listing, filtering, matching, and replacement.

This is useful when writing custom \code{exclude} functions, comparing paths
with seekr results, or building file vectors before calling
\code{\link[=filter_files]{filter_files()}} or \code{\link[=match_files]{match_files()}}.
}
\details{
A \strong{seekr path} is a character path that has been:
\itemize{
\item expanded, so \code{~} is resolved,
\item normalized, so redundant path components are removed,
\item resolved to an absolute path,
\item represented with forward slashes.
}
\subsection{Where seekr paths are created}{

\code{\link[=list_files]{list_files()}} returns seekr paths: it starts from user-supplied
directories and returns the listed files as normalized absolute paths.

\code{\link[=filter_files]{filter_files()}} and \code{\link[=match_files]{match_files()}} normalize their input \code{path} before
filtering or matching. This means path-based filters such as
\code{path_pattern} are applied to seekr paths, regardless of whether the input
paths were originally relative, absolute, or written with
platform-specific separators.
}

\subsection{Why this matters}{

Normalizing paths makes path filtering more predictable. A \code{path_pattern}
is matched against a stable representation of the file path instead of
depending on how the path was originally written by the user.
}
}
\examples{
as_seekr_path(".")
as_seekr_path(c(".", "~"))

}
\seealso{
\code{\link[=list_files]{list_files()}}, \code{\link[=filter_files]{filter_files()}}, \code{\link[=match_files]{match_files()}}
}
