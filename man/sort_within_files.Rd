% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seekr-match.R
\name{sort_within_files}
\alias{sort_within_files}
\title{Sort matches within each file}
\usage{
sort_within_files(x)
}
\arguments{
\item{x}{A \code{\link{seekr_match}} vector.}
}
\value{
A \code{\link{seekr_match}} vector with the same matches in potentially
different order: files appear in their original order, and matches within
each file are sorted by \code{start}, then \code{end}, then \code{match}, then
\code{replacement}.
}
\description{
\code{sort_within_files()} sorts matches by position within each file, while
preserving the order in which files appear in \code{x}.

This differs from \code{sort()}, which sorts globally and reorders files
alphabetically. Use \code{sort_within_files()} when you want positions within
each file to be consistent — for example, after combining two
\code{\link{seekr_match}} vectors with \code{\link[vctrs:vec_c]{vctrs::vec_c()}} — without changing the order
in which files are displayed or processed.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
x <- seekr("TODO", path = ext_path)
y <- seekr("FIXME", path = ext_path)

# Combine and reorder within files without changing file order
z <- vctrs::vec_c(x, y)
sort_within_files(z)

}
