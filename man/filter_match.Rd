% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seekr-match.R
\name{filter_match}
\alias{filter_match}
\title{Filter matches}
\usage{
filter_match(x, ...)
}
\arguments{
\item{x}{A \code{\link{seekr_match}} vector.}

\item{...}{One or more filtering expressions, evaluated against the fields
of \code{x}. Field names (\code{path}, \code{match}, \code{start_line}, \code{replacement}, etc.)
can be used directly. Each expression must return a logical vector of the
same length as \code{x}, with no missing values. Multiple expressions are
combined with \code{&}.}
}
\value{
A \code{\link{seekr_match}} vector containing only the matches for which all
expressions evaluated to \code{TRUE}. If no expressions are supplied, \code{x} is
returned unchanged.
}
\description{
\code{filter_match()} subsets a \code{\link{seekr_match}} vector using expressions
evaluated directly against its fields, without needing to call
\code{\link[vctrs:field]{vctrs::field()}} explicitly.

The two calls below are equivalent:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{x[field(x, "start_line") > 10]
filter_match(x, start_line > 10)
}\if{html}{\out{</div>}}

\code{filter_match()} is modelled after \code{\link[dplyr:filter]{dplyr::filter()}}: field names can be
used directly in expressions, and multiple expressions are combined with
\code{&}. This makes it easy to write readable multi-condition filters:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{x |> filter_match(
  grepl("/R/", path),
  match == "TODO",
  start_line > 10
)
}\if{html}{\out{</div>}}
}
\section{Differences from base R subsetting}{

Unlike \code{x[condition]}, \code{filter_match()} does not recycle logical vectors.
Each expression must return exactly \code{length(x)} values. Missing values
(\code{NA}) in any expression cause an error. This prevents silent mistakes from
implicit recycling or incomplete conditions.
}

\examples{
ext_path <- system.file("extdata", package = "seekr")
x <- seekr("TODO|FIXME", path = ext_path)

# Filter by line number
filter_match(x, start_line > 10)

# Filter by file path
filter_match(x, grepl("/R/", path))

# Filter by matched text
filter_match(x, match == "TODO")

# Combine multiple conditions
filter_match(x, match == "TODO", start_line > 10, grepl("/R/", path))

# Equivalent base R subsetting (more verbose)
x[
  field(x, "match") == "TODO" &
  field(x, "start_line") > 10 &
  grepl("/R/", field(x, "path"))
]

}
\seealso{
\itemize{
\item \code{\link[vctrs:field]{vctrs::field()}} to access a field directly for use in base R subsetting.
\item \code{\link[=as_tibble.seekr_match]{as_tibble.seekr_match()}} and \code{\link[=as_match]{as_match()}} for more complex workflows that
require tabular manipulation.
\item \code{\link[=replace_files]{replace_files()}} to apply staged replacements after filtering.
}
}
