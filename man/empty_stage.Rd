% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seek.R
\name{empty_stage}
\alias{empty_stage}
\title{Diagnose where a workflow became empty}
\usage{
empty_stage(x)
}
\arguments{
\item{x}{A \code{\link{seekr_match}} object, returned by either \code{\link[=seek]{seek()}} or \code{\link[=seekr]{seekr()}}.}
}
\value{
Either \code{NULL} for non-empty results, or one of \code{"input"}, \code{"list"},
\code{"filter"}, or \code{"match"} for empty results.
}
\description{
\code{empty_stage()} retrieves the pipeline stage that produced an empty
\code{\link{seekr_match}} result returned by \code{\link[=seek]{seek()}} or \code{\link[=seekr]{seekr()}}.

Empty results can happen at different stages of the pipeline:
\itemize{
\item \code{"input"}: the input \code{path} was empty.
\item \code{"list"}: no files were found by \code{\link[=list_files]{list_files()}}.
\item \code{"filter"}: all files were excluded by \code{\link[=filter_files]{filter_files()}}.
\item \code{"match"}: files were searched, but no match was found.
}

For non-empty results, \code{empty_stage()} returns \code{NULL}.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
x <- seek("pattern_that_does_not_exist", path = ext_path)
empty_stage(x)

}
