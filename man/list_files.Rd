% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list.R
\name{list_files}
\alias{list_files}
\title{List files to search}
\usage{
list_files(
  path = ".",
  ...,
  recurse = TRUE,
  all = FALSE,
  use_git = FALSE,
  .progress = seekr_option("seekr.progress")
)
}
\arguments{
\item{path}{A character vector of one or more existing directories to
search in. Defaults to \code{"."} (the current working directory).}

\item{...}{These dots are for future extensions and must be empty.}

\item{recurse}{Controls how deep the directory traversal goes to list files. Either:
\itemize{
\item \code{TRUE} (default): recurse into all subdirectories.
\item \code{FALSE}: only list files at the top level of each directory in \code{path}.
\item A positive integer: limit recursion to that many levels deep.
}}

\item{all}{Whether to list hidden files and directories. Default is \code{FALSE}.}

\item{use_git}{Should Git be used to restrict file discovery inside Git
repositories? If \code{TRUE}, \code{\link[=list_files]{list_files()}} keeps only files that were first
discovered according to \code{path}, \code{recurse}, and \code{all}, and are also returned
by \verb{git ls-files --cached --others --exclude-standard}. \code{use_git = TRUE} looks
for the Git root by walking upward from each supplied \code{path}, but it does not
recursively search downward for Git repositories in subdirectories. Git must
be installed and available on \code{PATH}.}

\item{.progress}{Whether to display progress messages. Default is \code{TRUE} in
interactive sessions and \code{FALSE} otherwise (see \code{\link[rlang:is_interactive]{rlang::is_interactive()}}).
Can be set globally with \code{options(seekr.progress = FALSE)}.}
}
\value{
A character vector of normalized absolute file paths. Returns an
empty character vector if no files are found or if \code{path} is empty.
}
\description{
\code{\link[=list_files]{list_files()}} starts from \code{path} and lists candidate files. It can recurse
into subdirectories with \code{recurse}, include hidden files and directories with
\code{all}, and optionally restrict discovery inside Git repositories with
\code{use_git}. It is the first step of the \code{\link[=seek]{seek()}} pipeline.

Listing is intentionally simple: it does not know about patterns, extensions,
file sizes, or MIME types. Its only job is to turn directories into a
character vector of file paths. Filtering happens in the next step,
\code{\link[=filter_files]{filter_files()}}.

If \code{use_git = TRUE}, Git is used for each input path independently.
For each path, \code{\link[=list_files]{list_files()}} asks Git whether that path is inside a Git
repository. If it is, \code{\link[=list_files]{list_files()}} finds the repository root by walking
upward from that path, then keeps only the files also returned by
\verb{git ls-files --cached --others --exclude-standard} for that repository.

Git is used to restrict the files discovered from the input path. It does not
expand the search. The \code{path}, \code{recurse}, and \code{all} arguments still define the
initial candidate files. For example, Git-tracked hidden files are not
returned unless \code{all = TRUE}, and Git-tracked files below the requested
recursion depth are not returned.

\code{\link[=list_files]{list_files()}} does not search downward for nested Git repositories. If an
input path is not inside a Git repository, it is listed normally, even if it
contains Git repositories in subdirectories. If you want Git-aware discovery
for nested repositories, pass those repository directories explicitly in
\code{path}.

If \code{use_git = TRUE}, Git must be installed and available on \code{PATH}.

The returned paths are normalized as described in \code{\link[=as_seekr_path]{as_seekr_path()}}.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")

# List all files in the example directory
list_files(path = ext_path)

# List only files at the top level, without recursing
list_files(path = ext_path, recurse = FALSE)

# Recurse at most 2 levels deep
list_files(path = ext_path, recurse = 2L)

# Include hidden files and directories
list_files(path = ext_path, all = TRUE)

\dontrun{
# Use Git to restrict discovery inside Git repositories
list_files(path = ".", use_git = TRUE)
}

}
\seealso{
\itemize{
\item \code{\link[=filter_files]{filter_files()}} to filter the listed files before searching matches.
\item \code{\link[=seek]{seek()}} to run the full listing, filtering, and matching pipeline.
}
}
