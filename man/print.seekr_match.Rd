% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.seekr_match}
\alias{print.seekr_match}
\title{Print matches with context and replacement preview}
\usage{
\method{print}{seekr_match}(x, ..., n = NULL, context = 0L)
}
\arguments{
\item{x}{A \code{\link{seekr_match}} vector.}

\item{...}{Not used. Present for compatibility with the \code{print()} generic.}

\item{n}{Maximum number of matches to print. If \code{NULL}, a compact default is
used: all matches are printed for small vectors, and only the first matches
are printed for larger vectors. Use \code{Inf} to print all matches.}

\item{context}{Number of context lines to print around each match. Either:
\itemize{
\item A single non-negative integer: print that many lines before and after
each match.
\item A pair of non-negative integers \code{c(before, after)}: print \code{before}
lines before and \code{after} lines after each match.
}

Only context lines captured when the \code{\link{seekr_match}} vector was created can
be printed.}
}
\value{
Invisibly returns the original \code{\link{seekr_match}} vector \code{x}.
}
\description{
\code{print()} displays a \code{\link{seekr_match}} vector in a readable console format,
grouped by source. Each printed match is shown with its source, match index,
line number, matched text, and, when available, its staged replacement.

The amount of output can be controlled with \code{n}, which limits the number of
matches printed. \code{context} controls how many surrounding lines are
shown.
}
\examples{
ext_path <- system.file("extdata", package = "seekr")
x <- seekr(
  pattern = "(\\\\w+)(?= <- function)",
  replacement = toupper,
  path = ext_path
)

# Print up to 10 matches (default)
print(x)

# Print all matches
print(x, n = Inf)

# Print only 3 matches
print(x, n = 3L)

# Reduce context to 1 line around each match
print(x, context = 1L)

# Show 3 lines before and 1 line after each match
print(x, context = c(3L, 1L))

}
\seealso{
\link[=summary.seekr_match]{summary()} for a compact summary of matches,
\code{\link[=filter_match]{filter_match()}} to subset matches before printing, and \code{\link[=seek]{seek()}} for the
\code{context} argument that controls how many surrounding lines are captured.
}
