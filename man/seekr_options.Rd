% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{seekr_options}
\alias{seekr_options}
\title{List seekr options}
\usage{
seekr_options()
}
\value{
A tibble with one row per seekr option and the following columns:
\itemize{
\item \code{name}: option name.
\item \code{current}: value currently set by the user, or \code{NA} if unset.
\item \code{default}: default value used by seekr when the option is unset.
}
}
\description{
\code{seekr_options()} returns the options that control seekr's global behavior,
along with their current user-defined value and their package default.

These options can be changed with \code{\link[base:options]{base::options()}}. For example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(seekr.progress = FALSE)
options(seekr.print.mode = "plain")
}\if{html}{\out{</div>}}
\subsection{Option values}{

The \code{current} column reports the value currently set with \code{\link[base:options]{base::options()}}.
If an option has not been set by the user, \code{current} is \code{NA} and seekr falls
back to the value shown in \code{default}.
}

\subsection{Available options}{

The main options are:
\itemize{
\item \code{seekr.progress}: whether seekr displays progress messages by default.
\item \code{seekr.backup_dir}: directory where backups are stored.
\item \code{seekr.print.mode}: print mode, either \code{"rich"}, \code{"color"}, or \code{"plain"}.
\item \code{seekr.print.tab}: symbol used to display tab characters.
\item \code{seekr.print.newline}: symbol used to display newline characters when printing deleted newlines.
\item \verb{seekr.style.*}: ANSI style codes used internally by rich printing.
}

The \verb{seekr.style.*} options are intentionally low-level. They accept ANSI SGR
codes as strings, such as \code{"31"}, \code{"1;31"}, or \code{"38;5;243"}.
}
}
\examples{
seekr_options()

# Disable progress messages globally
options(seekr.progress = FALSE)
seekr_options()

# Reset the option
options(seekr.progress = NULL)

}
\seealso{
\code{\link[=seekr_option]{seekr_option()}} to retrieve the resolved value of a single option.
}
